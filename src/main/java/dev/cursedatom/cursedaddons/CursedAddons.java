package dev.cursedatom.cursedaddons;

import net.fabricmc.api.ModInitializer;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Main mod initializer for CursedAddons.
 * Runs on both client and server sides during mod loading.
 */
public class CursedAddons implements ModInitializer {
	public static final String MOD_ID = "cursedaddons";

	public static final Logger LOGGER = LoggerFactory.getLogger(MOD_ID);

	@Override
	public void onInitialize() {
	}
}
